<h1>Products</h1>

<%= link_to "New product", new_product_path %>

<div class="products">
    <% @products.each do |product| %>
        <div class="product">
            <%= link_to product.name, product %>
        </div>
    <% end %>
</div>
